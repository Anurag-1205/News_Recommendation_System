# Pinned environment — every package `make env` installs, at the exact version on the machine
# where the suite was last verified green (Python 3.12.3). Pinned so both team members' `make env`
# resolve to byte-for-byte the same versions (CONTEXT.md C-011). To change a version: change it
# here, run `make test` on a fresh venv, and log the change in CONTEXT.md.
#
# NOTE: scikit-learn and rank-bm25 are ORACLE ONLY. BM25 is hand-written (SPEC.md §10, decision 4);
# rank_bm25 exists to disagree with ours and expose bugs, not to be the implementation.
#
# requirements-embed.txt (torch, transformers) is deliberately NOT pinned here: the local torch is
# a CPU build (2.13.0+cpu) that PyPI cannot serve, and Kaggle's GPU image supplies its own CUDA torch.

# --- Direct dependencies -----------------------------------------------------------------------
polars==1.43.2
pyarrow==25.0.1
numpy==2.5.2
scikit-learn==1.9.0        # ORACLE ONLY — roc_auc_score cross-check
faiss-cpu==1.15.0
rank-bm25==0.2.2           # ORACLE ONLY — cross-check our BM25 ranking
pytest==9.1.1
tqdm==4.70.0
matplotlib==3.11.1
huggingface_hub[cli]==1.28.0
kaggle==2.2.4              # CLI: push code/data as datasets, run kernels on Kaggle T4s (CONTEXT.md C-004)

# --- Transitive dependencies, pinned for exact reproducibility ---------------------------------
# The full dependency closure of the packages above, as installed. Not imported directly by our code.
anyio==4.14.2
attrs==26.1.0
bleach==6.4.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.4.2
contourpy==1.3.3
cycler==0.12.1
fastjsonschema==2.22.2
filelock==3.32.3
fonttools==4.63.0
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyter-core==5.9.1
jupytext==1.19.5
kagglesdk==0.1.37
kiwisolver==1.5.0
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
mdurl==0.1.2
narwhals==2.25.0
nbformat==5.11.1
packaging==26.3
pillow==12.3.0
platformdirs==4.11.8
pluggy==1.6.0
polars-runtime-32==1.43.2
protobuf==7.36.1
pygments==2.21.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-slugify==9.0.0
pyyaml==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scipy==1.18.0
six==1.17.0
text-unidecode==1.3
threadpoolctl==3.6.0
traitlets==5.16.1
typing-extensions==4.16.0
urllib3==2.7.0
webencodings==0.6.1
